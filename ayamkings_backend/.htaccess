# AyamKings Backend - Apache Config
# Only handle OPTIONS preflight - Let PHP handle CORS headers

RewriteEngine On

# Handle OPTIONS preflight requests with 200 OK
RewriteCond %{REQUEST_METHOD} OPTIONS
RewriteRule ^(.*)$ $1 [R=200,L]
